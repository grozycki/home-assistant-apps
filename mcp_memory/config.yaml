name: "MCP Memory"
description: "Local MCP memory server with vector search for Assist."
version: "1.0.0"
slug: "mcp_memory"
init: false
arch:
  - aarch64
  - amd64
  - armhf
  - armv7
  - i386
startup: services
ports:
  8000/tcp: 8000
options:
  default_category: "general"
schema:
  default_category: str